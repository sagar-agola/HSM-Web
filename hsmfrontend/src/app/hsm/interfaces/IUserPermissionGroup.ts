export interface IUserPermissionGroup {
    id: number,
    groupName: string,
    isActive: boolean
}

export interface IUserPermissionGroupModel {
    id: number,
    groupName: string,
    userStatus: string,
}